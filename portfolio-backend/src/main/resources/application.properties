spring.application.name=portfolio-backend
server.port=${PORT:8080}

# MongoDB Atlas
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://portfolioadmin:Thirushan20072002@cluster0.q2ffq2j.mongodb.net/portfolio_db?retryWrites=true&w=majority&appName=Cluster0}
spring.data.mongodb.database=portfolio_db

# MongoDB connection tuning (fail fast in dev)
spring.data.mongodb.auto-index-creation=true

# JWT
app.jwt.secret=${JWT_SECRET:ThisIsAVeryLongSecretKeyForJWTTokenGenerationPortfolio2024!}
app.jwt.expiration=86400000

# Admin credentials
app.admin.username=${ADMIN_USERNAME:admin}
app.admin.password=${ADMIN_PASSWORD:admin123}

# CORS
app.cors.allowed-origins=http://localhost:3000,https://thirushan2007.github.io

# Logging
logging.level.com.portfolio=DEBUG
logging.level.org.springframework.security=WARN